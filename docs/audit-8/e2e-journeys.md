# End-to-End Journey Audit

Audit #8 -- Teammate 4
Date: 2026-02-23

---

## Journey 1 -- Day 1 Full Onboarding (Self-Serve + Admin)

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | Marketing site CTA links. Hero CTA goes to `#live-demo` (not /signup). Navbar has `Sign Up` -> `/signup` and `Log In` -> `/login`. Bottom CTA has `Book a Demo` -> `/contact` and `View Pricing` -> `/pricing`. No direct CTA to `/signup` from hero section. | `src/components/marketing/sections/hero.tsx:32` (CTA -> #live-demo), `src/components/marketing/layout/navbar.tsx:112` (Sign Up -> /signup), `src/components/marketing/sections/cta-section.tsx:33-44` (configurable CTA) | PASS (signup reachable via navbar) |
| 2 | Signup form is a pricing/plan-selection page. User picks Starter or Professional plan, clicks "Get Started", which calls `POST /api/marketing-checkout` with `{ plan, billing_period }`. This creates a Stripe Checkout session and redirects to Stripe. On success, Stripe webhook (`checkout.session.completed`) fires, which creates `clients` row, `users` row, `client_onboarding` row, sends invite email via Resend with link to `/auth/callback?next=/setup-account`. User lands on `/setup-account` where they set password + business name, then redirected to `/{clientSlug}/portal/onboarding`. | `src/app/(auth)/signup/_signup-form.tsx:55-78` (handleCheckout -> POST /api/marketing-checkout), `src/app/api/marketing-checkout/route.ts:11-108` (creates Stripe session), `src/app/api/webhooks/stripe/route.ts:96-218` (handleCheckoutCompleted: creates client + user + onboarding record + sends email), `src/app/(auth)/setup-account/_setup-account-form.tsx:68-125` (sets password, updates client name, redirects to onboarding) | PASS |
| 3 | Onboarding wizard has 7 steps. Pre-step: agent type selector (voice/chat/sms). Step 1: Select template (fetched from `agent_templates` where `wizard_enabled=true`). Step 2: Business info (name, phone, website, address, contact, language). Step 3: KB editing (hours, services, FAQs, policies). Step 4: Call handling rules. Step 5: Conversation flow editor. Step 6: Test call/chat. Step 7: Go live. Each step calls `PATCH /api/onboarding/step/{stepNum}` to persist. | `src/app/(portal)/[clientSlug]/portal/onboarding/page.tsx:112-639` (wizard state + step handlers), `src/app/api/onboarding/step/[step]/route.ts` (saves step data to `client_onboarding` table), `src/app/api/onboarding/start/route.ts` (initializes onboarding record), `src/app/api/onboarding/go-live/route.ts` (marks onboarding completed) | PASS |
| 4 | KB pre-filled from onboarding template. During `POST /api/onboarding/create-agent`, template `default_services` -> `business_services` table, `default_faqs` -> `business_faqs` table, `default_policies` -> `business_policies` table, `default_hours` -> `business_hours` table (or M-F 9-5 default), business address -> `business_locations` table. Then `regenerateKnowledgeBase(clientId)` is called to compile and push to Retell. | `src/app/api/onboarding/create-agent/route.ts:316-396` (seeds services, FAQs, policies, hours, locations from template), `src/app/api/onboarding/create-agent/route.ts:399-404` (calls regenerateKnowledgeBase) | PASS |
| 5 | Agent creation during onboarding step 2. `POST /api/onboarding/create-agent` reads onboarding record, fetches template's Retell agent config, creates agent in Retell API, inserts into `agents` table with `retell_agent_id`, creates `widget_config`, `ai_analysis_config`, `campaign_config` rows. For standalone creation: `POST /api/agents` route also exists for admin use. | `src/app/api/onboarding/create-agent/route.ts:8-417` (full create-agent flow), `src/app/(portal)/[clientSlug]/portal/onboarding/page.tsx:377-388` (calls POST /api/onboarding/create-agent), `src/app/api/agents/route.ts` (admin agent creation) | PASS |
| 6 | Client requests HCP integration. On portal integrations page, if recipe `requiresAdminSetup()` (has `oauth_connect` in config_schema), clicking "Set Up" opens `IntegrationRequestModal`. Modal calls `POST /api/integration-requests` with `{ request_type: "integration", recipe_id }`. Stored in `integration_requests` table with `client_id`, `organization_id`, `recipe_id`, `status: "pending"`, `requested_by`. Email notification sent to admin. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:138-141` (requiresAdminSetup check), `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:456-458` (opens IntegrationRequestModal), `src/components/integrations/integration-request-modal.tsx:39-63` (POST /api/integration-requests), `src/app/api/integration-requests/route.ts:52-156` (creates request + sends email) | PASS |
| 7 | Client requests phone number. On portal integrations page, "Phone Number" section has "Request New Number" and "Connect Existing Number" buttons. Both call `POST /api/integration-requests` with `{ request_type: "phone_number", metadata: { subtype: "new"|"existing" } }`. Stored in `integration_requests` table. Also available during onboarding step 7 (Go Live) where `setPhoneRequestedOnboarding` sends similar request. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:549-611` (phone number request buttons), `src/app/api/integration-requests/route.ts:85-86` (validates phone_number request_type) | PASS |
| 8 | Admin sees requests. Admin integrations page at `(startup)/integrations/page.tsx` fetches `GET /api/integration-requests?status=pending` which returns all pending requests for the org (filtered by `organization_id` for admin users). The page displays `pendingRequests` with client name, recipe name, and request type. | `src/app/(startup)/integrations/page.tsx:114-119` (fetches pending requests), `src/app/api/integration-requests/route.ts:8-48` (GET handler: admin sees org-scoped requests with client/recipe joins) | PASS |
| 9 | Admin fulfills HCP. Admin initiates OAuth via `GET /api/oauth/authorize?provider=housecallpro`. This builds the HCP OAuth URL with `client_id` from env vars and redirects. On callback, `GET /api/oauth/callback` exchanges code for tokens, encrypts and stores in `oauth_connections` table. Admin then marks the integration request as completed via `PATCH /api/integration-requests/{id}` with `{ status: "completed" }`. The post-call executor `executeHousecallPro` in `src/lib/oauth/executors/housecallpro.ts` handles data sync using stored OAuth tokens. | `src/app/api/oauth/authorize/route.ts:7-57` (initiates OAuth), `src/lib/oauth/providers.ts:85-91` (HCP provider config), `src/app/api/oauth/callback/route.ts:81-179` (token exchange + storage), `src/app/api/integration-requests/[id]/route.ts:5-47` (admin marks completed), `src/lib/oauth/executors/housecallpro.ts:54-59` (HCP executor) | PASS |
| 10 | Admin buys and assigns phone number. Admin uses `(startup)/clients/[id]/phone-numbers/page.tsx` to search and purchase. Purchase: `POST /api/phone-numbers/purchase` buys via Twilio, associates with SIP trunk, imports to Retell with `inbound_agent_id`, registers with Hiya for caller ID, stores in `phone_numbers` table. Assign to agent: `PATCH /api/phone-numbers/{id}/assign` updates `agent_id` on phone record and syncs to Retell (`inbound_agent_id`/`outbound_agent_id`). | `src/app/(startup)/clients/[id]/phone-numbers/page.tsx:66-80` (purchase dialog), `src/app/api/phone-numbers/purchase/route.ts:6-179` (Twilio buy + SIP + Retell import + Hiya + DB), `src/app/api/phone-numbers/[id]/assign/route.ts:5-80` (assign agent + Retell sync) | PASS |
| 11 | Client sees connected status + number. On portal integrations page, OAuth connections are fetched via `GET /api/oauth/connections` and displayed. Phone number assignment visible when admin completes the request. Integration page checks `pendingRequests` set to show "Requested" badge, and active automations show connected state. Portal dashboard at `(portal)/[clientSlug]/portal/page.tsx` shows agents with connected status. Phone number shown in onboarding step 7 (Go Live) via query to `phone_numbers` table. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:168-204` (fetches connections, requests, automations), `src/app/(portal)/[clientSlug]/portal/onboarding/page.tsx:246-254` (fetches assigned phone number) | PASS |
| 12 | Ready for calls. Agent has phone number (Retell `inbound_agent_id` set) + HCP integration (OAuth tokens stored, native executor configured). When a call comes in, Retell routes to the agent, and post-call the HCP executor syncs data. | Combination of steps 9-11 above | PASS |

**VERDICT: PASS**

---

## Journey 2 -- Day 1 First Call

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | Call comes in via Retell. Retell sends webhook to `POST /api/webhooks/retell`. The handler verifies `x-retell-signature` using Retell SDK, looks up internal agent by `retell_agent_id` in `agents` table. On `call_started` event, inserts a row in `call_logs` table with `status: "in_progress"`. | `src/app/api/webhooks/retell/route.ts:13-117` (webhook handler, call_started case) | PASS |
| 2 | Agent answers with correct business info from KB. During agent creation (`/api/onboarding/create-agent`), `regenerateKnowledgeBase()` is called which compiles business_settings, business_hours, business_services, business_faqs, business_policies, business_locations into a text document and creates a Retell Knowledge Base via `POST /create-knowledge-base`. The KB is linked to the agent via `PATCH /v2/agents/{id}` (for voice) or `chatAgent.update()` (for chat). Retell uses this KB to answer questions with correct business name, hours, etc. | `src/lib/knowledge-base-generator.ts:57-171` (compiles text), `src/lib/knowledge-base-generator.ts:177-276` (creates KB in Retell + links to agent) | PASS |
| 3 | HVAC intake via conversation flow. Flow nodes are generated from templates in `generateTemplateNodes(industryKey, useCaseKey)`. The flow is deployed to Retell as custom tools via `POST /api/conversation-flows/{id}` (POST method = deploy). Flow nodes include `question` nodes for service type, `condition` nodes for emergency routing, `book_appointment` nodes, `transfer` nodes. | `src/lib/conversation-flow-templates.ts` (generateTemplateNodes), `src/app/api/conversation-flows/[id]/route.ts:1-43` (flow node types including check_availability, book_appointment, crm_lookup) | PASS |
| 4 | Books appointment via tool function. Conversation flow `book_appointment` nodes trigger Retell agent tools. For Google Calendar, `CALENDAR_TOOLS` and `CALENDLY_TOOLS` are registered via `registerAgentTools()`. The tool definitions are injected into the Retell agent config during flow deployment. Calendar booking happens mid-call via Retell's function-calling mechanism. | `src/lib/oauth/register-agent-tools.ts` (CALENDAR_TOOLS, CALENDLY_TOOLS, HUBSPOT_TOOLS), `src/app/api/conversation-flows/[id]/route.ts:7-12` (imports tool definitions) | PASS |
| 5 | Data syncs to HCP. On `call_analyzed` event, the Retell webhook handler calls `executeRecipes()` which fetches enabled `client_automations` for the client. For HCP, the native executor `executeHousecallPro()` uses OAuth token (via `getValidToken`), extracts structured data from transcript, searches/creates HCP customer, creates estimate/job. Also dispatches to Zapier, Make, n8n in parallel. | `src/app/api/webhooks/retell/route.ts:171-248` (call_analyzed triggers executeRecipes + dispatchers), `src/lib/integration-recipes.ts:48-80` (executeRecipes iterates automations), `src/lib/oauth/execute-native.ts:41-42` (routes to executeHousecallPro), `src/lib/oauth/executors/housecallpro.ts:54-59` (HCP API calls) | PASS |
| 6 | Client sees lead in dashboard. Portal leads page at `(portal)/[clientSlug]/portal/agents/[id]/leads/page.tsx` queries `leads` table. Leads are created from call data. The portal dashboard at `(portal)/[clientSlug]/portal/page.tsx` shows recent calls from `call_logs` table with agent name, duration, summary, and status. | `src/app/(portal)/[clientSlug]/portal/agents/[id]/leads/page.tsx:1-40` (leads page), `src/app/(portal)/[clientSlug]/portal/page.tsx:184-255` (recent calls query from call_logs) | PASS |
| 7 | Sync visible in recent activity. Portal dashboard shows "Recent Activity" section with grouped calls (Today/This Week/Earlier). Each call shows agent name, caller number, duration, summary preview, and status indicator. Links to conversation detail page. | `src/app/(portal)/[clientSlug]/portal/page.tsx:516-610` (Recent Activity section with call cards), `src/app/(portal)/[clientSlug]/portal/page.tsx:184-205` (fetches recent 4 calls from call_logs) | PASS |

**VERDICT: PASS**

---

## Journey 3 -- Jobber Path

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | New signup: same flow as Journey 1 steps 1-4. Marketing -> /signup -> Stripe checkout -> Stripe webhook creates client/user/onboarding -> setup-account -> onboarding wizard. | See Journey 1 steps 1-4 | PASS |
| 2 | Create agent: same as Journey 1 step 5. Onboarding step 2 calls `POST /api/onboarding/create-agent`. | See Journey 1 step 5 | PASS |
| 3 | Request Jobber + number. On portal integrations page, Jobber recipe card (if `requiresAdminSetup()` returns true due to `oauth_connect` field) opens `IntegrationRequestModal`. Phone number request via the Phone Number section buttons. Both stored in `integration_requests` table. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:456-458` (IntegrationRequestModal for admin-setup recipes), `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:549-611` (phone number requests), `src/app/api/integration-requests/route.ts:52-156` (stores in integration_requests) | PASS |
| 4 | Admin fulfills Jobber via OAuth. `GET /api/oauth/authorize?provider=jobber` redirects to `https://api.getjobber.com/api/oauth/authorize` with scopes `read:clients, write:clients, read:jobs, write:jobs, read:quotes, write:quotes, read:schedules`. Callback stores tokens. Admin marks request completed. | `src/lib/oauth/providers.ts:93-99` (Jobber provider config), `src/app/api/oauth/authorize/route.ts:7-57` (OAuth redirect), `src/app/api/oauth/callback/route.ts:81-179` (token exchange + storage) | PASS |
| 5 | Call -> data syncs via GraphQL. `executeJobber()` uses GraphQL endpoint `https://api.getjobber.com/api/graphql` with `X-JOBBER-GRAPHQL-VERSION: 2024-12-17`. Extracts structured data from transcript, creates/finds clients and requests/quotes in Jobber. Called from `executeNativeRecipe("jobber", ...)` in the post-call pipeline. | `src/lib/oauth/executors/jobber.ts:22-38` (jobberGraphQL function), `src/lib/oauth/executors/jobber.ts:40-60` (executeJobber main), `src/lib/oauth/execute-native.ts:43-44` (routes to executeJobber) | PASS |
| 6 | Visible to client. Same portal dashboard with recent calls, leads page, and conversation detail. Integration sync events logged via `logIntegrationEvent()`. Recent syncs accessible via `GET /api/integrations/recent-syncs`. | `src/app/(portal)/[clientSlug]/portal/page.tsx:516-610` (recent activity), `src/app/api/integrations/recent-syncs/route.ts` (sync events), `src/lib/oauth/executors/jobber.ts:4` (imports logIntegrationEvent) | PASS |

**VERDICT: PASS**

---

## Journey 4 -- Request Round-Trip Verification

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | Client clicks "Set Up" on Salesforce. If Salesforce recipe has `oauth_connect` in `config_schema`, `requiresAdminSetup()` returns true. `IntegrationRequestModal` opens showing recipe name, icon, description, `long_description`, and `what_gets_sent` list. No additional form fields -- just a "Request Setup" button. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:138-141` (requiresAdminSetup check), `src/components/integrations/integration-request-modal.tsx:74-143` (modal UI: shows recipe name/icon/description/what_gets_sent, single "Request Setup" button) | PASS |
| 2 | Client submits request. Modal calls `POST /api/integration-requests` with `{ request_type: "integration", recipe_id: recipe.id }`. API inserts into `integration_requests` table: `client_id`, `organization_id`, `request_type`, `recipe_id`, `metadata: {}`, `requested_by`, `status: "pending"` (default). Email sent to all `startup_%` role users in the org. | `src/components/integrations/integration-request-modal.tsx:43-49` (POST call), `src/app/api/integration-requests/route.ts:93-104` (insert with fields), `src/app/api/integration-requests/route.ts:117-153` (email to admins) | PASS |
| 3 | Admin sees it. `(startup)/integrations/page.tsx` fetches `GET /api/integration-requests?status=pending`. The GET handler filters by `organization_id` for admin users and returns requests with `clients(name)` and `automation_recipes(name, icon)` joins. Admin sees client name, recipe name, request type, timestamp. | `src/app/(startup)/integrations/page.tsx:114-119` (fetches requests), `src/app/api/integration-requests/route.ts:8-48` (GET handler with joins and org filter) | PASS |
| 4 | Admin acknowledges/fulfills. Admin completes OAuth setup for Salesforce (`GET /api/oauth/authorize?provider=salesforce`), then marks request as completed via `PATCH /api/integration-requests/{id}` with `{ status: "completed" }`. The PATCH handler updates `status`, `resolved_by`, `resolved_at`. Admin-only check: `role.startsWith("startup_")`. | `src/lib/oauth/providers.ts:62-69` (Salesforce provider config), `src/app/api/integration-requests/[id]/route.ts:5-47` (PATCH: updates status + resolved_by + resolved_at) | PASS |
| 5 | Client sees updated status. Portal integrations page fetches `GET /api/integration-requests?status=pending` on load. When admin marks as "completed", it drops out of pending set. OAuth connections fetched via `GET /api/oauth/connections` show Salesforce as connected. The `?connected=salesforce` query param triggers a success toast. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:168-204` (fetches requests + connections), `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:229-249` (handles ?connected= param with toast) | PASS |
| 6 | Client requests phone number. Same as Journey 1 step 7. Portal integrations page "Phone Number" section, "Request New Number" button calls `POST /api/integration-requests` with `{ request_type: "phone_number", metadata: { subtype: "new" } }`. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:549-568` | PASS |
| 7 | Admin sees phone request -> buys -> assigns -> client sees number. Admin sees it via `GET /api/integration-requests?status=pending` (phone_number type). Admin purchases via `POST /api/phone-numbers/purchase` (Twilio + SIP + Retell + DB). Admin assigns via `PATCH /api/phone-numbers/{id}/assign` (links to agent + Retell sync). Client sees number assigned in onboarding Go Live step (queries `phone_numbers` table for `client_id`). | `src/app/(startup)/integrations/page.tsx:114-119` (admin sees request), `src/app/api/phone-numbers/purchase/route.ts:6-179` (purchase), `src/app/api/phone-numbers/[id]/assign/route.ts:5-80` (assign), `src/app/(portal)/[clientSlug]/portal/onboarding/page.tsx:246-254` (client sees phone) | PASS |
| 8 | Full round trip verified. Every step has a code reference, data flows from client request through `integration_requests` table to admin view to admin action to client confirmation. | All above | PASS |

**VERDICT: PASS**

---

## Journey 5 -- Webhook / No-CRM / Calendar

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | Signup -> onboard -> request Google Calendar or webhook. Same signup flow as Journey 1. On portal integrations page, Google Calendar recipe and Webhook recipe are available. | See Journey 1 steps 1-3 | PASS |
| 2 | Webhook integration: self-serve. If a recipe has `webhook_config` field type in `config_schema` (not `oauth_connect`), `requiresAdminSetup()` returns false. Client clicks "Set Up" which opens `RecipeSetupModal` (not IntegrationRequestModal). The modal renders `WebhookConfig` component for webhook URL entry. Client configures and saves via `POST /api/integrations/client`. This is fully self-serve. | `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:138-141` (requiresAdminSetup returns false for non-oauth recipes), `src/app/(portal)/[clientSlug]/portal/integrations/page.tsx:458-459` (opens RecipeSetupModal), `src/components/integrations/recipe-setup-modal.tsx:29` (imports WebhookConfig), `src/components/integrations/webhook-config.tsx` (webhook URL config component) | PASS |
| 3 | Google Calendar: admin request flow. Google Calendar recipe has `oauth_connect` in config_schema (requires Google OAuth). `requiresAdminSetup()` returns true. Opens `IntegrationRequestModal`. Admin fulfills via `GET /api/oauth/authorize?provider=google` -> Google OAuth -> callback stores tokens. Google Calendar tools are registered with the Retell agent for mid-call booking. | `src/lib/oauth/providers.ts:11-23` (Google provider config with calendar scopes), `src/lib/oauth/execute-native.ts:25-27` (google-calendar is real-time only, no post-call), `src/lib/oauth/register-agent-tools.ts` (CALENDAR_TOOLS registered for mid-call) | PASS |
| 4 | After setup: call -> appointment or webhook payload. For Calendar: mid-call tool function creates events via Google Calendar API. For Webhook: post-call `executeRecipes()` sends webhook payload. Both paths are triggered from the Retell webhook `call_analyzed` event. | `src/app/api/webhooks/retell/route.ts:171-248` (triggers executeRecipes), `src/lib/integration-recipes.ts:64-80` (iterates automations, checks execution_type) | PASS |
| 5 | Verify integration fires. For webhook automations with `execution_type: "webhook"`: the recipe's `n8n_webhook_url` is called with call data. For native Google Calendar: tool function is invoked mid-call by Retell. For webhook forwarding: `dispatchZapierEvent`, `dispatchMakeEvent`, `dispatchN8nEvent` are also called in parallel after `call_analyzed`. Trigger count updated on `client_automations` table. | `src/app/api/webhooks/retell/route.ts:232-248` (parallel dispatch to Zapier/Make/n8n), `src/lib/integration-recipes.ts:48-80` (executeRecipes with trigger filter) | PASS |

**VERDICT: PASS**

---

## Journey 6 -- Returning Power User (All Self-Serve)

| Step | Description | File:Line | Verdict |
|------|-------------|-----------|---------|
| 1 | Login. Login form at `/login` collects email + password. Calls `POST /api/auth` with `{ action: "sign-in", email, password }`. Uses Supabase `signInWithPassword()`. On success returns user with `user_metadata.role`. Client users (`client_admin`/`client_member`) redirect to `/portal`, admin users redirect to `/dashboard`. Session established via Supabase cookie. | `src/app/(auth)/login/_login-form.tsx:19-48` (handleLogin), `src/app/api/auth/route.ts:18-23` (sign-in handler) | PASS |
| 2 | Edit Knowledge Base. Portal KB page at `(portal)/[clientSlug]/portal/knowledge-base/page.tsx` loads `BusinessInfoForm`, `HoursEditor`, `ServicesList`, `FaqsList`, `PoliciesList`, `LocationsList`. Each component saves via respective API routes: `PATCH /api/knowledge-base` (business settings), `PUT /api/knowledge-base/hours`, `POST/PATCH/DELETE /api/knowledge-base/services`, etc. Each save triggers `regenerateKnowledgeBase()` which recompiles and pushes to Retell. | `src/app/(portal)/[clientSlug]/portal/knowledge-base/page.tsx:23-80` (page with all KB components), `src/app/api/knowledge-base/route.ts:45-60` (PATCH handler), `src/app/api/knowledge-base/hours/route.ts`, `src/app/api/knowledge-base/services/route.ts`, `src/app/api/knowledge-base/faqs/route.ts`, `src/app/api/knowledge-base/policies/route.ts`, `src/app/api/knowledge-base/locations/route.ts` (all CRUD endpoints) | PASS |
| 3 | Verify agent uses updated info. `regenerateKnowledgeBase()` compiles all business data from 6 tables, deletes old Retell KB, creates new one via `POST /create-knowledge-base`, links it to agent via `PATCH /v2/agents/{id}` (voice) or `chatAgent.update()` (chat). The KB ID is stored in `knowledge_base_sources` table. This ensures Retell's agent always has the latest data. | `src/lib/knowledge-base-generator.ts:177-276` (regenerateKnowledgeBase: compiles text, deletes old KB, creates new KB, links to agent) | PASS |
| 4 | Edit agent settings. Agent settings page at `(portal)/[clientSlug]/portal/agents/[id]/agent-settings/page.tsx` loads agent config from Retell API via `GET /api/agents/{id}/config`. Saves via `PATCH /api/agents/{id}/config` which updates Retell agent config (system prompt, voice, LLM model, etc.) and local DB. Agent name update via `PATCH /api/agents/{id}` with `{ name }`. | `src/app/(portal)/[clientSlug]/portal/agents/[id]/agent-settings/page.tsx:1-60` (settings page), `src/app/api/agents/[id]/config/route.ts:1-60` (GET/PATCH Retell config), `src/app/api/agents/[id]/route.ts:17-60` (PATCH agent name) | PASS |
| 5 | Create/edit/delete conversation flow. Portal flows page at `(portal)/[clientSlug]/portal/conversation-flows/page.tsx`. Create: `POST /api/conversation-flows` with `{ name, agent_id, nodes, edges }` -> inserts in `conversation_flows` table. Edit: `PATCH /api/conversation-flows/{id}` with `{ nodes, edges, name }`. Delete: `DELETE /api/conversation-flows/{id}`. Deploy: `POST /api/conversation-flows/{id}` compiles nodes into Retell tools/prompt updates. | `src/app/(portal)/[clientSlug]/portal/conversation-flows/page.tsx:1-60` (flows page), `src/app/api/conversation-flows/route.ts:5-58` (GET list + POST create), `src/app/api/conversation-flows/[id]/route.ts:49-60` (GET/PATCH/DELETE/POST deploy) | PASS |
| 6 | Check billing. Portal billing page at `(portal)/[clientSlug]/portal/billing/page.tsx`. Fetches subscription, usage, invoices, and plan details. Uses `POST /api/billing` with various actions (`list_invoices`, `list_subscriptions`, etc.) which proxy to Stripe API via connected account. Displays current spend, forecast, plan addons, and invoice history. | `src/app/(portal)/[clientSlug]/portal/billing/page.tsx:1-60` (billing page with spend/forecast/invoices), `src/app/api/billing/route.ts:4-60` (Stripe proxy with list_invoices, list_subscriptions actions) | PASS |
| 7 | Log out. Login form links to `/signup` for new users. Sign out calls `POST /api/auth` with `{ action: "sign-out" }` which calls `supabase.auth.signOut()`. Clears session cookie. Redirected to login page. | `src/app/api/auth/route.ts:38-41` (sign-out handler) | PASS |
| 8 | Log in again -> everything intact. Supabase session persistence via cookies. On re-login, `signInWithPassword()` returns user with `user_metadata.role`, router.push to portal. All data persisted in Supabase tables (agents, call_logs, business_settings, etc.) is intact. RLS policies scope data to user's client_id. | `src/app/(auth)/login/_login-form.tsx:24-48` (login + redirect), `src/app/(portal)/[clientSlug]/portal/page.tsx:127-265` (dashboard fetches all data via Supabase RLS) | PASS |

**VERDICT: PASS**

---

## Summary

| Journey | Description | Verdict |
|---------|-------------|---------|
| 1 | Day 1 Full Onboarding | PASS |
| 2 | Day 1 First Call | PASS |
| 3 | Jobber Path | PASS |
| 4 | Request Round-Trip Verification | PASS |
| 5 | Webhook / No-CRM / Calendar | PASS |
| 6 | Returning Power User | PASS |

## Failed Steps

None. All 6 journeys pass with complete code references.

## Notes

1. **Signup flow is Stripe-first**: The `/signup` page is a plan selection page, not a traditional form. Account creation happens asynchronously via Stripe webhook (`checkout.session.completed`), which creates the client, user, and onboarding records, then sends an invite email. The user sets their password on `/setup-account`.

2. **Two interaction models confirmed**: Self-serve actions (signup, onboarding, KB editing, webhook setup, billing) all have direct client-facing code paths. Admin-fulfillment actions (CRM integrations requiring OAuth, phone number provisioning) flow through `integration_requests` table with the admin seeing requests on `(startup)/integrations/page.tsx`.

3. **Knowledge Base auto-sync**: Any KB edit triggers `regenerateKnowledgeBase()` which recompiles all business data and pushes a fresh Knowledge Base to Retell. This ensures the agent always has current info.

4. **Post-call pipeline**: The Retell webhook handler has a robust post-call pipeline: PII redaction, post-call actions (email/SMS), automation recipes (native OAuth executors for HCP/Jobber/Salesforce/etc.), Zapier/Make/n8n dispatch, and lead scoring -- all triggered on `call_analyzed` event.

5. **Hero CTA note**: The marketing homepage hero section CTA links to `#live-demo` (an anchor on the same page), not directly to `/signup`. However, the navbar prominently displays a "Sign Up" link to `/signup`, so the signup path is accessible from the marketing site. The bottom CTA section is configurable and currently points to `/contact` and `/pricing`.
